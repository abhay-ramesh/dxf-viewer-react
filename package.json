{
  "name": "dxf-viewer-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter dxf-viewer-react build && bun run --filter dxf-viewer-react-demo build",
    "lint": "bun run --filter '*' lint"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^6.0.5"
  },
  "packageManager": "bun@1.3.14"
}
